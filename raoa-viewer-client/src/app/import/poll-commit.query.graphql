query PollCommit($id: ID!){
    pollCommitState(jobId: $id){
        album {
            id
        }
        currentPhase
        currentStep
        totalStepCount
        files {
            fileId
        }
    }
}
